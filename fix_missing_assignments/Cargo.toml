[package]
name = "fix_missing_assignments"
version = "0.1.0"
edition = "2021"

[workspace]

[dependencies]
sqlx = { version = "0.8", features = ["runtime-tokio-native-tls", "sqlite"] }
tokio = { version = "1", features = ["full"] }
