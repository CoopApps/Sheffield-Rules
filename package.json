{
  "name": "saturday-at-three",
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "npm -C frontend run dev",
    "build": "npm -C frontend run build",
    "tauri": "npx tauri",
    "tauri-dev": "npm run build && npx tauri dev",
    "tauri-build": "npm run build && npx tauri build"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.10.1"
  },
  "dependencies": {
    "better-sqlite3": "^12.6.2",
    "csv-parse": "^6.1.0",
    "sqlite3": "^5.1.7",
    "xlsx": "^0.18.5"
  }
}
